## Running the Program

Run `main.py` from the project root to see a full demonstration. It reads from `data/TestData.csv`, prints the top scorers to STDOUT, and writes all records to the database:

```bash
python src/main.py
```